# Grafana 数据源自动配置（Provisioning）
# 部署到容器 /etc/grafana/provisioning/datasources/ 目录
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
